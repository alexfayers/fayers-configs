# fayers-configs

The configs that I use for linting stuff. Ships as python package so usage is easier.

## Usage

```sh
uv add --group dev git+https://github.com/alexfayers/fayers-configs
uv run fayers-configs
```
